{% extends "base.html" %}
{% block content %}
  {% if session.logged_in %}
  <div class="row">
	  <div class="col-md-8 col-md-offset-2">
    <form action="{{ url_for('edit_entry', post_title=entry.title) }}" method=post class=add-entry>
	    <div class="form-group">
		    <label for="title">Title</label>
		    <input type=text class="form-control" name=title value="{{ entry.title }}">
	    </div>
	    <div class="form-group">
		    <label for=text>Text</label>
		    <textarea class="form-control"name=text >{{ entry.text }}</textarea>
	    </div>
	    <button type=submit class="btn btn-default">Share</button>
    </form>
    </div>
    </div>
  {% endif %}

{% endblock %}
